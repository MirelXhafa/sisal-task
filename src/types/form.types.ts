import {TPersonalInformation} from "@/types/PersonalInformation.types";
import {TAccountInformation} from "@/types/AccountInformation.types";

export type TFormData = {
    personalInformation: TPersonalInformation
    accountInformation: TAccountInformation
}